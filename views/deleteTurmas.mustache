<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Deletar Turma</title>
  <script>
    function confirmDelete() {
      var confirmed = window.confirm("Tem certeza que deseja deletar esta turma?");
      return confirmed;
    }
  </script>
</head>
<body>
  <h1>Deletar Turma</h1>
  <form method="POST" action="/api/turmas/delete" onsubmit="return confirmDelete()">
    <label for="codigo">Selecione o turma a ser deletada:</label>
    <select name="codigo" id="codigo">
      {{#turmas}}
      <option value="{{codigo}}">{{codigo}} ({{materia_codigo}})</option>
      {{/turmas}}
    </select>
    <br>
    <button type="submit">Deletar</button>
  </form>
  <br><a href="/">Página Inicial</a>
</body>
</html>
